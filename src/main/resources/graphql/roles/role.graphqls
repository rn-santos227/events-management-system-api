extend type Query {
  roles(limit: Int): [Role!]!
  role(id: ID!): Role
}

type Role {
  id: ID!
  name: String
  privileges: [PrivilegeSummary!]!
  createdAt: String
  updatedAt: String
}

type RoleSummary {
  id: ID!
  name: String
}
